# Configuração do Banco de Dados (Docker Local)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=celilac_db
DB_USER=celilac_user
DB_PASSWORD=celilac_password

# Autenticação JWT
JWT_SECRET=MUDE_ESTE_VALOR_EM_PRODUCAO
JWT_EXPIRES_IN=7d

# Servidor
PORT=3000
